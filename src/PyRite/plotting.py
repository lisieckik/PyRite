import matplotlib.pyplot as plt

def set_default_style():
    # """Apply the custom matplotlib style shipped with this package."""
    # style_path = pathlib.Path(mytools.__file__).parent / "styles" / "default.mplstyle"
    # plt.style.use(style_path)
    return 1